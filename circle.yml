general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore
deployment:
  production:
    branch: master
    commands:
      - npm install
      - npm install -g gulp
      - gulp build
