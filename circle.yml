general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore
deployment:
  production:
    branch: master
    commands:
      - npm install
      - npm install -g gulp
      - gulp build
      - git checkout -b gh-pages
      - git merge master
      - git commit -a -m "Circle CI"
